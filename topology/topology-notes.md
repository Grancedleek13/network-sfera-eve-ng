# Topology notes

Здесь предполагается хранить:

- экспорт схемы сети из draw.io (`drawio-network-diagram.drawio`);
- при желании — PNG‑изображение схемы (`network-topology.png`);
- дополнительные схемы (физическая/логическая топология).

В репозитории лежит только заглушка – файл нужно заменить своей актуальной схемой.
